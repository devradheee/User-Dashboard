# User Dashboard App (React Frontend Assignment)

This is a simple **User Dashboard** web application built using **React.js** as part of a frontend intern assignment.

The app displays a list of users fetched from an API, allows searching users by name, and supports adding new users locally.

---

## 🚀 Features

- Fetch users from API (JSONPlaceholder)
- Display users in a responsive card layout
- Search/filter users by name
- Create new user form (client-side only)
- Global state management using React Context API
- User Details page using React Router
- Mobile-friendly responsive design

---

## 🛠 Tech Stack

- React.js (Functional Components + Hooks)
- Axios (API Requests)
- React Router DOM (Routing)
- Context API (Global State)
- Basic CSS for styling

---


